<div class="map-container">
    <img src="MAPA.png" alt="Mapa" class="map">
    <img src="logoblack.png" alt="Logotipo" class="logo">
</div>

<style>
    .map-container {
        position: relative;
        width: 100%;
        max-width: 100vw;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden;
        padding: 0 1rem;
        animation: slideInFromLeft 1.2s ease-out;
        box-sizing: border-box;
    }

    .map {
        width: 100%;
        max-width: 1200px;
        height: auto;
        display: block;
    }

    .logo {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        max-width: 65rem;
        width: 110%;
        height: auto;
        z-index: 2;
        animation: slideInFromLeftCentered 1.5s ease-out 0.3s both;
    }

    /* Animaciones de entrada */
    @keyframes slideInFromLeft {
        0% {
            opacity: 0;
            transform: translateX(-100px);
        }
        100% {
            opacity: 1;
            transform: translateX(0);
        }
    }
    
    @keyframes slideInFromLeftCentered {
        0% {
            opacity: 0;
            transform: translate(-50%, -50%) translateX(-100px);
        }
        100% {
            opacity: 1;
            transform: translate(-50%, -50%) translateX(0);
        }
    }

    /* Responsive Design para el mapa y logo */
    @media (max-width: 1024px) {
        .logo {
            max-width: 45rem;
            width: 85%;
        }
    }

    @media (max-width: 768px) {
        .map-container {
            padding: 0 0.5rem;
        }
        
        .logo {
            max-width: 35rem;
            width: 80%;
        }
    }

    @media (max-width: 480px) {
        .map-container {
            padding: 0;
        }
        
        .logo {
            max-width: 28rem;
            width: 75%;
        }
    }

    @media (max-width: 320px) {
        .logo {
            max-width: 22rem;
            width: 70%;
        }
    }
</style>

<div>
    <h3>At <span>Media Digital Group</span>, we believe in the power of ideas… transforming bold visions into thriving businesses.</h3>
</div>

<style>
    div h3{
        font-size: 2rem;
        width: 50%;
        margin: auto;
        font-weight: 300;
        color: #3f3f3f;
        line-height: 1.3;
        text-align: center;
        animation: fadeInUp 1s ease-out 1.8s both;
    }
    
    @keyframes fadeInUp {
        0% {
            opacity: 0;
            transform: translateY(30px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    div h3 span{
        font-weight: 600;
    }
    
    /* Responsive Design - Solo para móviles */
    @media (max-width: 768px) {
        div h3 {
            width: 80%;
            font-size: 1.6rem;
            padding: 0 1rem;
        }
    }
    
    @media (max-width: 480px) {
        div h3 {
            width: 90%;
            max-width: 320px;
            font-size: 1.4rem;
            line-height: 1.4;
            padding: 0 1.5rem;
        }
    }
    
    @media (max-width: 320px) {
        div h3 {
            width: 95%;
            max-width: 280px;
            font-size: 1.2rem;
            line-height: 1.5;
            padding: 0 1rem;
        }
    }
</style>